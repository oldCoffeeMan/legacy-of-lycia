name: CI

on:
  push:
    branches: [ main, sprint-* ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e backend/src/lycia
          pip install pytest pytest-cov httpx jsonschema
          pip install ruff mypy

      - name: Lint with Ruff
        run: |
          cd backend/src/lycia
          ruff check .
        continue-on-error: false

      - name: Type check with Mypy
        run: |
          cd backend/src/lycia
          mypy . --install-types --non-interactive
        continue-on-error: true  # Allow to continue for now

      - name: Run tests with pytest
        run: |
          cd backend
          PYTHONPATH=src pytest tests/ -v --cov=lycia --cov-report=term-missing

      - name: Upload coverage reports
        uses: codecov/codecov-action@v4
        with:
          files: ./backend/.coverage
          fail_ci_if_error: false
        continue-on-error: true
